[pytest]
asyncio_mode = auto
markers =
    integration: hits real Azure OpenAI / Azure AI Search (cost + latency; skipped automatically when credentials aren't configured). Exclude with `-m "not integration"`.
